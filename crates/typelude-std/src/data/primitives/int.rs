//! **Integer Data Types**
//!
//! Re-exports from `typenum`.
//! `Eval` is implemented in `typelude-core` for these types.

pub use typenum::{
    // Type Operators (for convenience)
    Add1,
    B0,
    B1,
    // Operations (Traits)
    Bit,
    Integer,
    NInt,
    NonZero,
    PInt,
    Sub1,
    UInt,
    UTerm,
    Unsigned,
    Z0,
};
